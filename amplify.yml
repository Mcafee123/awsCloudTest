version: 1
frontend:
    phases:
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: /frontend/formkit/dist
        files:
            - '**/*'
    cache:
        paths: []
